# parity-generator-checker
A **Parity Generator and Checker** is a digital circuit used to detect errors in binary data during transmission. It generates and checks a parity bit to identify single-bit errors in the data.
